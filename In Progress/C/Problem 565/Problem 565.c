#include <stdio.h>
#include <math.h>

long SumOfDivisors(long number)
{
    long sum;
    long stopping = sqrt(number);
    for (long i = 2
    return 0;
}

int main(int argc, char **argv)
{
    return 0;
}

